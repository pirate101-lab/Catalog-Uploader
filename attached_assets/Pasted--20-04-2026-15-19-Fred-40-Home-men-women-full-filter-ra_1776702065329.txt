[20/04/2026 15:19] Fred: #40 - Home: men/women + full filter rail on the left
What & Why

The homepage today shows a small left rail with a category list and a compact filter widget. The user wants the homepage to behave more like a real clothing store landing: a clear Men / Women gender switch at the top of the left rail, the full set of shop filters underneath, and a product grid on the right. On mobile this collapses into a single "Filters" drawer the user already opens with the existing button.
Done looks like

    The homepage left rail (visible on desktop, drawer on mobile) shows in this order, top to bottom:
        A prominent Women / Men / All gender toggle (segmented control).
        The category list (dresses, tops, etc.) that already exists.
        The full product filter panel: sizes, price range, color, sort.
    Active filter count is visible on the rail header so the operator can see at a glance how filtered the grid is, with a "Clear all" link.
    The product grid stays on the right and continues to support infinite scroll. Empty / loading states are unchanged.
    On screens < lg, the rail collapses into the existing mobile filters drawer with the same content and order. The "Filters" button shows a small badge with the active filter count.
    Selecting a category, gender, or filter updates the URL query string the way it does today (so deep links keep working).
    No regressions to the hero slider, the announcement bar, or the "View all" link to the Shop page.

Out of scope

    Changing the Shop page (/shop) — only the homepage rail changes.
    Adding new filter facets (brand, material, etc.).
    Server-side filter changes; everything stays client-side via the existing useProducts search.

Steps

    Pull the gender toggle out of HomeFilters into the rail header so it sits above the category list as a standalone segmented control. Keep the existing gender filter state and URL sync.
    Reorder the rail content to: gender toggle → category list → remaining filters (sizes / price / color / sort). Add an "Active filters: N · Clear all" line at the top.
    Reuse the same rail in the mobile drawer so desktop and mobile show identical controls. Add the active-filter badge to the "Filters" trigger button.
    Verify the URL ↔️ state round-trip still works for category, gender, sizes, price, sort.
    Visual QA at common breakpoints (375, 768, 1024, 1440px) and confirm sticky positioning on desktop is unchanged.

Relevant files

    artifacts/fashion/src/pages/Home.tsx:300-470
    artifacts/fashion/src/components/HomeFilters.tsx
    artifacts/fashion/src/components/CategoryRail.tsx
    artifacts/fashion/src/hooks/useHomeFilters.ts
    artifacts/fashion/src/lib/shopHref.ts
[20/04/2026 15:22] Fred: #41 - Calmer hero glow + a touch more blue background
What & Why

The hero slider currently has a strong radial blue "glow" behind the text plus very saturated tri-color text gradients, which can read more like a club poster than a fashion store. The user wants the glow gone (or much subtler) while keeping the brand vibe. Separately, they want the page background to feel ~5% bluer to push the boutique-blue identity without going into "tinted" territory.
Done looks like

    The radial blue glow behind the hero text is removed (or reduced to a barely-visible scrim that only improves text legibility).
    The hero kicker / headline still feel premium but read in a single brand color or a much subtler gradient — no rainbow effect.
    The site's light-mode page background is noticeably (but tastefully) bluer — roughly a 5% bump in the brand-indigo tint and a slightly stronger mesh gradient on hero / section backgrounds.
    Dark mode is left to the existing dark-palette refresh task and is not touched here.
    Body copy contrast against the new background still meets WCAG AA.

Out of scope

    Dark mode palette changes (covered by existing draft "Refresh dark mode to match the new vibrant blue palette").
    Restyling the hero typography itself or replacing imagery.
    Changing brand primary or accent colors elsewhere.

Steps

    Remove the hero radial-glow layer behind the slider text and keep only the existing top/bottom legibility scrims.
    Soften the hero text gradients — replace the multi-stop rainbow on kicker and headline with a single brand color (or a 2-stop gradient inside the brand blue family).
    Bump the page background blue tint by ~5% in light mode (CSS variable in index.css) and slightly increase the alpha of the blue/indigo stops in .bg-mesh so section backgrounds pick up the change too.
    Contrast check body and muted text against the new background and nudge if needed to stay AA.
    Visual QA the home, shop, PDP, cart, checkout, and admin chrome to confirm the new background reads correctly.

Relevant files

    artifacts/fashion/src/components/HeroSlider.tsx:120-260
    artifacts/fashion/src/index.css:80-130,280-320
[20/04/2026 15:24] Fred: #42 - Production polish for mobile + desktop store
What & Why

Before pointing shopthelook.page at this site, the storefront needs a focused responsive + correctness sweep: the hero shouldn't hijack small-phone viewports, tap targets should be thumb-sized, the new left-rail homepage layout should feel intentional on every breakpoint, and any console warnings, broken images, or 404s spotted along the way should be fixed. This is the "ready for a real domain" pass — not a visual redesign.
Done looks like

    Every major page (home, shop, product, cart, checkout, account orders, admin shell) renders cleanly at 360px, 414px, 768px, 1024px, 1280px, and 1440px without horizontal scrolling, overlapping elements, or broken sticky positioning.
    All interactive controls on mobile have ≥ 44×44px tap targets (filter chips, cart steppers, qty inputs, header icons).
    Hero slider on phones uses a sensible height (not 100dvh), keeps the CTA buttons above the fold, and the autoplay does not jank scroll.
    Product cards never overflow their grid cell; image aspect ratio is locked so the page doesn't reflow during image load.
    The header (announcement + nav + search) collapses gracefully on small screens; the search input becomes a sheet/overlay rather than fighting for row width.
    Cart and checkout forms are usable one-handed: inputs autofill correctly (autocomplete attributes), keyboard types are right (inputmode for postal code / phone), and the pay button is reachable above the mobile keyboard.
    All known browser-console warnings on the storefront are resolved (key warnings, missing alt text, hydration mismatches, deprecated React APIs).
    A quick admin smoke pass: the admin shell sidebar collapses on mobile, settings page (with the new test-email button) is usable on a phone.
    Lighthouse mobile (or equivalent quick audit) shows no critical accessibility or best-practices regressions vs. today.

Out of scope

    New features.
    Dark-mode color refresh (covered by existing draft).
    Rounding remaining square chips (covered by existing draft).
    Backend / API changes — frontend polish only.
    Deployment to Digital Ocean (handled separately via the step-by-step the user receives in chat).

Steps

    Responsive audit pass — walk every major page at the listed breakpoints, screenshot each, and capture a punch list of every layout issue found.
    Fix the layout punch list — work through the issues, biasing toward small fixes (height, padding, grid-template adjustments, hidden toggles) rather than rewriting components.
    Tap-target + form-field polish — ensure controls meet 44×44px, add missing autocomplete / inputmode / name attributes on checkout and account forms.
    Hero on phones — switch from 100dvh to a phone-friendly height (~70dvh capped) and confirm CTAs stay visible.
    Console hygiene — open the storefront in the dev preview, log into admin, click through one full user journey (browse → PDP → add to cart → checkout) and resolve every warning the browser prints.
    Quick a11y check — make sure all images have alt text, all icon-only buttons have aria-label, and the new left-rail filters are reachable by keyboard.
    Sanity test the build (pnpm --filter @workspace/fashion build and pnpm --filter @workspace/api-server build) so we catch any type or bundling regressions before deploy.

Relevant files

    artifacts/fashion/src/pages/Home.tsx
    artifacts/fashion/src/pages/Shop.tsx
    artifacts/fashion/src/pages/Product.tsx
    artifacts/fashion/src/pages/Cart.tsx
    artifacts/fashion/src/pages/Checkout.tsx
    artifacts/fashion/src/components/HeroSlider.tsx
    artifacts/fashion/src/components/Header.tsx
    artifacts/fashion/src/components/AnnouncementBar.tsx
    artifacts/fashion/src/admin/AdminShell.tsx
    artifacts/fashion/src/index.css

Dependencies
home: men/women+full filter